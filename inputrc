# For more read: man 3 readline
###############################

# do not bell on tab-completion
set bell-style none

# enable eight-bit input (likely to depend on the LC_CTYPE locale category)
set input-meta on
set convert-meta off
set output-meta on

# enable coloring of completion of files by types
set colored-stats on

# causes readline to display all possible completions immediately after a single Tab key press
set show-all-if-ambiguous on

# To enable case-insensitive tab completion in Bash
set completion-ignore-case on

#################### VIM ####################
# FOR MORE INFORMATION CHECK:
# https://wiki.archlinux.org/index.php/Readline

# TURN ON VIM (E.G. FOR READLINE)
set editing-mode vi

$if mode=vi

# SHOW THE VIM MODE IN THE PROMPT (COMMAND OR INSERT)
set show-mode-in-prompt on

# SET THE MODE STRING AND CURSOR TO INDICATE THE VIM MODE
#   FOR THE NUMBER AFTER `\e[`:
#     0: blinking block
#     1: blinking block (default)
#     2: steady block
#     3: blinking underline
#     4: steady underline
#     5: blinking bar (xterm)
#     6: steady bar (xterm)
set vi-ins-mode-string \1\e[5 q\2
set vi-cmd-mode-string \1\e[2 q\2

$endif
